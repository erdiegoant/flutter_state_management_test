import 'package:flutter/material.dart';

class StackedEventsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stacked Events'),
      ),
      body: Center(
        child: Text('Stacked Events page'),
      ),
    );
  }
}
